# Object Info Extractor

Openplanet plugin used to extract block infos and item model ids from the game.
The extracted infos are saved as JSON files, which are used by the server.

## Usage

The plugin can be easily installed into the Openplanet directory using 
the `Install-ObjectInfoExtractor` PowerShell script.
