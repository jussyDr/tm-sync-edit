[package]
name = "tm-sync-edit-client-lib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
shared = { path = "../../../shared" }

futures = "0.3.30"
futures-util = { version = "0.3.30", features = ["sink"] }
native-dialog = "0.7.0"
tokio = { version = "1.36.0", features = ["macros", "net", "rt"] }
